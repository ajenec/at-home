//Practice with Strings+Arrays in functions. No numbers


const greet = (name) => {
    console.log(`${name}: Whats up buddy?`);
};

greet('Chuck');

const feeling = (str) => {
  if (str === 'sad'){
    return `${name}: `
  }
}